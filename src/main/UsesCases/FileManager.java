package main.UsesCases;

import main.Entity.*;
import java.util.*;
public class FileManager implements FileManagable {

    // Serialization in the future
    public LoginList readFile(String filepath){
        LoginList users = new LoginList();
        return users;
    }
}
